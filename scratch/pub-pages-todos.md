- Pages flash old order on page nav, move the swr local cache and maybe the nav to a layout since it's shared between pages anyway.

- Replace delete button w/ edit button (using edit icon) which opens a popover where users can edit the name and path of a page. There's also a delete button which changes the popover state to show an are you sure form with a cancel and delete button. Cancel switches the state back to editing. Delete has the same behavior as the current delete.

- Editor container isn't scrolling properly, toolbar moves off screen.
- Add signup form to the default home page
- make default home Home.
