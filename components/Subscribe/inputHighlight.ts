// The attention ring for a subscribe input the reader skipped (e.g. picking a
// membership tier before entering their email). Applied to the input wrapper
// while `highlight` is set and cleared on focus. Shared by EmailInput and
// HandleSearchInput so the two stay identical.
export const INPUT_HIGHLIGHT_CLASS =
  "border-accent-contrast outline-2 outline outline-accent-contrast outline-offset-1";
