// The header sits in the canvas's top-left corner, above the seeded first
// block (which components/Canvas.tsx places at x 8), inset by the same margin.
export const POST_HEADER_BLOCK_WIDTH = 440;
export const POST_HEADER_BLOCK_POSITION = { x: 8, y: 12 };
